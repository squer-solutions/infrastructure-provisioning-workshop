apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend
  namespace: argocd
spec:
  destination:
    name: in-cluster
    namespace: frontend
  project: default
  source:
    repoURL: ssh://git@github.com/squer-solutions/dummy-frontend.git
    targetRevision: main
    path: manifests
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true